// Equality operator is == (loose equality) and === (Strict equality)
// == only value will be same.
// === data and value both will be same

let num = 1;
let str = '1';

console.log(num == str);
console.log(num === str);